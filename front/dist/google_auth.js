// Generated by CoffeeScript 1.8.0
(function() {
  var AUTH_URL, GoogleLoginButtonDirective, googleAuthInfo, module;

  this.taigaContribPlugins = this.taigaContribPlugins || [];

  googleAuthInfo = {
    slug: "google-auth",
    name: "Google Auth",
    type: "auth",
    module: "taigaContrib.googleAuth",
    template: "contrib/google_auth"
  };

  this.taigaContribPlugins.push(googleAuthInfo);

  module = angular.module('taigaContrib.googleAuth', []);

  AUTH_URL = "https://accounts.google.com/o/oauth2/auth";

  GoogleLoginButtonDirective = function($window, $params, $location, $config, $events, $confirm, $auth, $navUrls, $loader) {
    var link;
    link = function($scope, $el, $attrs) {
      var clientId, loginOnError, loginOnSuccess, loginWithGoogleAccount;
      clientId = $config.get("googleClientId", null);
      loginOnSuccess = function(response) {
        var nextUrl;
        if ($params.next && $params.next !== $navUrls.resolve("login")) {
          nextUrl = $params.next;
        } else {
          nextUrl = $navUrls.resolve("home");
        }
        $events.setupConnection();
        $location.search("next", null);
        $location.search("token", null);
        $location.search("state", null);
        $location.search("code", null);
        return $location.path(nextUrl);
      };
      loginOnError = function(response) {
        $location.search("state", null);
        $location.search("code", null);
        $loader.pageLoaded();
        if (response.data.error_message) {
          return $confirm.notify("light-error", response.data.error_message);
        } else {
          return $confirm.notify("light-error", "Our Oompa Loompas have not been able to get you credentials from Google.");
        }
      };
      loginWithGoogleAccount = function() {
        var code, data, token, type;
        type = $params.state;
        code = $params.code;
        token = $params.token;
        if (!(type === "google" && code)) {
          return;
        }
        $loader.start();
        data = {
          code: code,
          token: token
        };
        return $auth.login(data, type).then(loginOnSuccess, loginOnError);
      };
      loginWithGoogleAccount();
      $el.on("click", ".button-auth", function(event) {
        var redirectToUri, url;
        redirectToUri = $location.url($location.path()).absUrl();
        url = "" + AUTH_URL + "?client_id=" + clientId + "&redirect_uri=" + redirectToUri + "&state=google&scope=openid%20email&response_type=code";
        return $window.location.href = url;
      });
      return $scope.$on("$destroy", function() {
        return $el.off();
      });
    };
    return {
      link: link,
      restrict: "EA",
      template: ""
    };
  };

  module.directive("tgGoogleLoginButton", ["$window", '$routeParams', "$tgLocation", "$tgConfig", "$tgEvents", "$tgConfirm", "$tgAuth", "$tgNavUrls", "tgLoader", GoogleLoginButtonDirective]);

  module.run([
    '$templateCache', function($templateCache) {
      return $templateCache.put('contrib/google_auth', '<div tg-google-login-button="tg-google-login-button"><a href="" title="Enter with your google account" class="button button-auth"><img src="images/contrib/google-logo.png"/><span>Login with Google</span></a></div>');
    }
  ]);

}).call(this);
